package kusu.trade.core_exchange.consts

enum class BiBitCategory(str: String) {
    LINEAR("linear"),
    INVERSE("inverse");

    companion object{
        private const val CATEGORY = "category"


    }
}